# Committed defaults, no secrets. Real env vars win over this file, so on a
# Vercel build the system variable replaces the value below.

# Build stamp in index.html. Vercel sets it to the deployed commit SHA.
VITE_VERCEL_GIT_COMMIT_SHA=dev

/** Playzone side length, in m. The square is centered on the origin. */
export const PLAYZONE_SIZE = 200;

/** Borderzone (concrete path) width around the playzone, in m. */
export const BORDER_WIDTH = 10;

/** Playzone tile side, in m. */
export const TILE_SIZE = 2;

/** Seed of the playzone's accent patch layout. */
export const PLAYZONE_SEED = 1;
